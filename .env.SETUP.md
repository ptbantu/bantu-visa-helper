# Bantu Visa Helper 环境变量配置指南

## 快速开始

### 1. 复制示例文件

```bash
cp .env.example .env.local
```

### 2. 填入你的配置值

编辑 `.env.local` 文件，填入以下信息：

## 必需的环境变量

### Supabase 配置（数据库）

从你的 Supabase 项目获取这些值：

```bash
# 从 Supabase Dashboard → Project Settings → Database
DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres

# 从 Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**如何获取 Supabase 凭据：**

1. 登录 [Supabase Dashboard](https://app.supabase.com)
2. 选择你的项目
3. 点击左侧 **Settings**（设置）
4. 点击 **Database** 选项卡
   - 复制 **Connection string** → `DATABASE_URL`
5. 点击 **API** 选项卡
   - 复制 **Project URL** → `SUPABASE_URL`
   - 复制 **anon** (public) key → `SUPABASE_ANON_KEY`

### AI API 密钥

#### Qwen VL（阿里云）

```bash
QWEN_VL_API_KEY=sk-...
```

**获取步骤：**

1. 登录 [阿里云 DashScope](https://dashscope.aliyun.com/)
2. 点击 **API 密钥** → **创建新的 API 密钥**
3. 复制密钥值

#### Gemini（Google）

```bash
GEMINI_API_KEY=AIzaSy...
```

**获取步骤：**

1. 访问 [Google AI Studio](https://aistudio.google.com/apikey)
2. 点击 **Create API Key**
3. 复制密钥值

### 应用配置

```bash
# 应用访问的 URL
APP_URL=http://localhost:3000
```

## Docker Compose 环境变量

当使用 Docker Compose 时，变量会从以下位置读取（按优先级）：

1. `.env.local` - 本地环境（优先级最高）
2. `.env` - 项目默认环境
3. `.env.docker` - Docker 专用配置

### Docker 开发环境

```bash
docker-compose -f docker-compose.dev.yml up
```

这会自动从 `.env.local` 或 `.env` 读取变量。

### Docker 生产环境

```bash
docker-compose -f docker-compose.prod.yml up -d
```

建议使用 Docker Secrets 或云平台的 Secrets 管理器。

## 完整配置示例

```bash
# .env.local（本地开发）

# ============================================
# Supabase 数据库
# ============================================
DATABASE_URL=postgresql://postgres:your_password@your-project.supabase.co:5432/postgres
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# AI API 密钥
# ============================================
QWEN_VL_API_KEY=sk-your-qwen-key
GEMINI_API_KEY=AIzaSy-your-gemini-key

# ============================================
# 应用配置
# ============================================
APP_URL=http://localhost:3000
FRONTEND_PORT=3000
NODE_ENV=development
```

## 验证配置

### 方式 1：检查 Next.js 能否连接数据库

```bash
npm run dev

# 打开浏览器访问 http://localhost:3000
# 如果应用正常加载，说明配置成功
```

### 方式 2：测试数据库连接

```bash
npx prisma db push

# 如果成功，说明 DATABASE_URL 配置正确
```

### 方式 3：测试 AI API

```bash
# 在应用中上传一张签证图片进行测试
# 如果能正确识别，说明 API 密钥配置成功
```

## 常见问题

### Q: Docker Compose 说 VERSION is obsolete

**A:** 这是 Docker Compose v2 的新行为。已在最新版本中修复，重新运行命令即可：

```bash
docker-compose -f docker-compose.dev.yml up
```

### Q: 缺少环境变量警告

**A:** 确保你已创建 `.env.local` 文件：

```bash
cp .env.example .env.local
# 编辑 .env.local 并填入实际值
```

### Q: 如何在不同环境使用不同的配置？

**A:** 使用 `.env` 文件优先级：

```bash
# 本地开发（优先级最高）
.env.local

# 全局配置
.env

# Docker 专用
.env.docker
```

### Q: 如何安全地管理生产环境的密钥？

**A:** 建议的方案：

1. **Docker Secrets（Docker Swarm）**
   ```bash
   docker secret create db_url /path/to/db_url.txt
   ```

2. **Kubernetes Secrets**
   ```bash
   kubectl create secret generic app-secrets \
     --from-literal=DATABASE_URL=...
   ```

3. **云平台 Secrets Manager**
   - AWS Secrets Manager
   - Google Cloud Secret Manager
   - Azure Key Vault

### Q: 不小心提交了 .env 文件怎么办？

**A:** 立即更改所有凭据：

```bash
# 1. 从 Git 历史中删除
git rm --cached .env
git commit -m "Remove .env file from history"

# 2. 更改所有 API 密钥和数据库密码
# （在相应的服务平台上操作）

# 3. 添加到 .gitignore
echo ".env*" >> .gitignore
git add .gitignore
git commit -m "Add .env files to gitignore"
```

## 安全建议

⚠️ **重要：**

- ✅ **要做：** 提交 `.env.example` 作为配置模板
- ✅ **要做：** 将 `.env*` 添加到 `.gitignore`
- ✅ **要做：** 为每个环境使用不同的密钥
- ✅ **要做：** 定期轮换 API 密钥

- ❌ **不要：** 将真实 `.env` 文件提交到 Git
- ❌ **不要：** 在代码中硬编码密钥
- ❌ **不要：** 通过 Slack/Email 分享密钥
- ❌ **不要：** 在公开的 GitHub 中暴露密钥

## 相关文档

- [Supabase 连接字符串](https://supabase.com/docs/guides/database/connecting-to-postgres)
- [Next.js 环境变量](https://nextjs.org/docs/basic-features/environment-variables)
- [Docker Compose 环境变量](https://docs.docker.com/compose/environment-variables/)
- [SECURITY_CHECKLIST.md](./SECURITY_CHECKLIST.md) - 完整的安全指南

---

**最后更新：** 2024-03-12
